
export class CloudUploader {
  private static readonly cloud = {}

  static readonly upload = async ({
    path,
    folderName,
  }: {
    path: string
    folderName: string
  })=> {
  }

  static readonly deleteFile = async (
    public_id: string,
  ) => {
  }
}
